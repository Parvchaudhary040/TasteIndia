<?xml version="1.0" encoding="utf-8"?>
<!-- Fully transparent: the Android 12+ system splash always needs an icon drawable, and
     omitting one falls back to the launcher icon. This keeps that first frame empty (just
     splash_background) so the Compose splash's knife/spoon animation is the only thing drawn. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
